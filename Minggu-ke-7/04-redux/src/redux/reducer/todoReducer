const initialState = {
    todos: [],
    isLoading:false,
    err:null
}


const todoReducer = (state = initialState, action) => {
    switch(action.type){
        default: return state;
    }
}

export default todoReducer;